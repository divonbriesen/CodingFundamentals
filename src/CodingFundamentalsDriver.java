public class CodingFundamentalsDriver
{

    public static void main(String[] args)
    {
        /*
         TODO: add more variable names don't necessarily have to be good names. add phrasing for first second third etc.
          replace multiple println with single println that prints a string variable FIX BUG IWTH SPAXCE AT BEGINNING
          next question type to add is random object instantiations (added them but can't decide if
            they're similar enough to group together or if they should be their own type of question
            At some point: log results to a text file with date and time, name, num of questions, score, correct answer/their answer
        */
        Menu menu = new Menu();
        menu.displayMenu("1");
    }
}